# Журнал проблем и решений (issues-log)

*Записи добавляются только по запросу пользователя*

## 🔍 012 - 2025-08-23 - UserDefaults синхронизация не работает между UI и фоновыми процессами - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Реализовать тоггл в меню для включения/отключения функции автоподмены слов
- **Окружение**: macOS, SwiftUI MenuBarExtra, CGEventTap фоновые процессы
- **Предпосылки**: UserDefaults использовался для хранения состояния, но UI и фоновые проверки работали в разных процессах

### ❌ **Проблема:**
- **Симптомы**: Тоггл в UI отображался корректно, но фоновые проверки всегда читали false независимо от состояния тоггла
- **Корневая причина**: SwiftUI @State с State(initialValue:) инициализируется один раз при создании view и не синхронизируется с UserDefaults
- **Влияние**: Функция автоподмены не могла быть включена/отключена через UI

### 🛠️ **Решение:**
- **Подход**: Заменить @State + onChange на @AppStorage для автоматической двусторонней синхронизации
- **Конкретные действия**: Заменил @State на @AppStorage("AutoDictionarySwitchEnabled"), убрал init() и onChange обработчик
- **Результат**: Тоггл работает корректно, состояние синхронизируется между UI и фоновыми процессами

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: @AppStorage решает проблему автоматически без дополнительных обработчиков
- **❌ Что не сработало**: @State + onChange (нет синхронизации), UserDefaults suite + synchronize() (domain mismatch)
- **💡 Уроки**: SwiftUI property wrappers лучше подходят для персистентных настроек чем ручное управление UserDefaults
- **🔄 Альтернативы**: NSUbiquitousKeyValueStore (для iCloud синхронизации), ручная синхронизация через NotificationCenter

### 🎯 **Применимость:**
- **Теги**: #swiftui #userdefaults #macos #config #state-management
- **Похожие ситуации**: Любые SwiftUI настройки с персистентностью в UserDefaults
- **Профилактика**: Использовать @AppStorage для всех пользовательских настроек в SwiftUI приложениях

## 🔍 001 - 2025-08-09 - Низкоприоритетный event tap не работает в Electron/VSCode - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Глобальный хоткей и перехват ввода для универсальной смены раскладки.
- **Окружение**: macOS Sonoma (Apple Silicon), Swift/SwiftUI, различные приложения (в т.ч. Electron/VSCode).
- **Предпосылки**: Изначально использовался `.cgSessionEventTap` и событийная модель Carbon.

### ❌ **Проблема:**
- **Симптомы**: Хоткей/перехват срабатывает в нативных приложениях, но не в VS Code.
- **Корневая причина**: Недостаточный приоритет источника перехвата событий; Electron-поле не всегда отдаёт события на сессии.
- **Влияние**: Смена раскладки не работала «повсеместно» — ключевое требование проекта.

### 🛠️ **Решение:**
- **Подход**: Перейти на `cghidEventTap` с высоким приоритетом перехвата.
- **Конкретные действия**: Реализован `SingleModifierHotkeyRecognizer` с переключением на `.cghidEventTap`, отмена одиночного нажатия при иных ключах/кликах.
- **Результат**: Стабильное срабатывание в VSCode и других «капризных» приложениях.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Высокоприоритетный HID‑tap надёжно ловит модификаторы.
- **❌ Что не сработало**: Carbon‑горячие клавиши для одиночного модификатора невозможны.
- **💡 Уроки**: Для универсальности предпочтительно HID‑уровень.
- **🔄 Альтернативы**: AX‑глобальные биндинги или перехват через Input Method — сложнее и избыточно.

### 🎯 **Применимость:**
- **Теги**: #backend #macos #input #hotkeys #electron #config
- **Похожие ситуации**: Любые Electron/Chromium приложения.
- **Профилактика**: Сразу выбирать HID‑tap для глобального перехвата.

## 🔍 002 - 2025-08-09 - Дублирующаяся логика замены текста приводит к регрессиям - 🏗️ Архитектурная

### 📊 **Контекст:**
- **Задача**: Унифицировать замену текста, чтобы избежать расхождений поведения.
- **Окружение**: `AccessibilityTextInteractor`, `SwitchAction`, Electron/VSCode.
- **Предпосылки**: Существовали параллельные пути: AX‑set, clipboard‑paste, type‑over‑selection, cut+type.

### ❌ **Проблема:**
- **Симптомы**: Дубликаты слов, отсутствующая замена выделенного, разные результаты в разных приложениях.
- **Корневая причина**: Несколько конкурирующих веток замены и тайминговые расхождения.
- **Влияние**: Непредсказуемость и регрессии.

### 🛠️ **Решение:**
- **Подход**: Единый универсальный путь.
- **Конкретные действия**: Убраны AX‑set/clipboard фоллбеки; для выделения — только cut+type (Cmd+X + HID‑печать); для невыделенного — Option+Delete + HID‑печать; восстановление boundary при committed словах.
- **Результат**: Стабильное поведение в нативных и Electron‑приложениях.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: cut+type надёжнее AX и clipboard.
- **❌ Что не сработало**: Прямая AX‑установка и вставка из буфера часто ломались в VSCode.
- **💡 Уроки**: Минимизировать зависимость от Clipboard/AX, использовать HID‑печать.
- **🔄 Альтернативы**: IME‑интеграция — сложнее, требует иной архитектуры.

### 🎯 **Применимость:**
- **Теги**: #backend #macos #accessibility #clipboard #architecture
- **Похожие ситуации**: Ввод в защищённых/нестандартных полях.
- **Профилактика**: Держать один «золотой» путь замены текста.

## 🔍 003 - 2025-08-09 - Потеря/дублирование текста из‑за самоперехвата наших событий - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Исключить влияние сгенерированных кейэвентов на трекинг «последнего слова».
- **Окружение**: `KeyboardMonitor`, `SwitchAction`, HID‑typing, Opt+Delete.
- **Предпосылки**: Без изоляции монитор видел наши же события ввода и портил буфер токена.

### ❌ **Проблема:**
- **Симптомы**: Дубли после конверсии, сброс или неправильный токен, не менялось выделение.
- **Корневая причина**: Отсутствие «suspend/resume» вокруг искусственных событий.
- **Влияние**: Нестабильная работа хоткея.

### 🛠️ **Решение:**
- **Подход**: Приостановка мониторинга на время отправки событий.
- **Конкретные действия**: Добавлены `suspend()`/`resume()` в `KeyboardMonitor`; `SwitchAction` оборачивает все KeySender‑операции.
- **Результат**: Пропали дубли, корректные токены до/после операции.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Простая контрактная изоляция событий.
- **❌ Что не сработало**: Попытки фильтровать события без явной паузы.
- **💡 Уроки**: Любые симулированные события должны быть исключены из мониторинга.
- **🔄 Альтернативы**: Маркировка событий userData — сложнее и менее переносима.

### 🎯 **Применимость:**
- **Теги**: #backend #events #keyboard #state
- **Похожие ситуации**: Автоматизация ввода тестов.
- **Профилактика**: Единый helper для операций «с паузой».

## 🔍 004 - 2025-08-09 - Потеря/добавление пробела после committed слова - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Сохранить точную пунктуацию/пробел после конверсии.
- **Окружение**: `KeyboardMonitor`, `SwitchAction`.
- **Предпосылки**: Неверное восстановление boundary символа (пробел/пунктуация).

### ❌ **Проблема:**
- **Симптомы**: То пропадает, то добавляется лишний пробел.
- **Корневая причина**: Не хранился тип boundary и не учитывалось наличие.
- **Влияние**: Визуальные артефакты в тексте.

### 🛠️ **Решение:**
- **Подход**: Snapshot токена и boundary на входе операции, точное восстановление только если boundary был.
- **Конкретные действия**: Перед удалением — Backspace для удаления boundary, после печати — возврат boundary, если он был whitespace.
- **Результат**: Корректное восстановление пробела/пунктуации без дубликатов.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Чёткая модель committed токена.
- **❌ Что не сработало**: «Умные» эвристики без явного snapshot.
- **💡 Уроки**: Фиксировать состояние до вмешательства.
- **🔄 Альтернативы**: Парсинг всего контекста строки — дороже и ненадежнее.

### 🎯 **Применимость:**
- **Теги**: #backend #tokenizer #text #ux
- **Похожие ситуации**: Автоправка после пунктуации.
- **Профилактика**: Единые правила работы с boundary.

## 🔍 005 - 2025-08-09 - Неполная конверсия спецсимволов и пунктуации - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Корректно конвертировать символы вроде `[ ] ; ' , . / ?` и их Shift‑варианты.
- **Окружение**: `LayoutConverter` (US↔RU), поведение при одиночных символах.
- **Предпосылки**: Изначально покрывались только буквы.

### ❌ **Проблема:**
- **Симптомы**: Одиночные и составные слова со спецсимволами конвертировались неверно или не конвертировались.
- **Корневая причина**: Неполные таблицы соответствий.
- **Влияние**: Потеря ожидаемого поведения.

### 🛠️ **Решение:**
- **Подход**: Расширить и выровнять таблицы соответствий под стандартные US↔RU раскладки.
- **Конкретные действия**: Добавлены все символы и их shifted‑пары; для одиночного символа — `Shift+Left` перед `typeText`.
- **Результат**: Конверсия одиночных и составных случаев стала предсказуемой.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Полная карта соответствий.
- **❌ Что не сработало**: Частичные маппинги.
- **💡 Уроки**: Пунктуация критична для UX.
- **🔄 Альтернативы**: Скрипт‑генерация таблиц из раскладок.

### 🎯 **Применимость:**
- **Теги**: #backend #layout #mapping #text
- **Похожие ситуации**: Любые языковые пары.
- **Профилактика**: Тесты на покрытие соответствий.

## 🔍 006 - 2025-08-09 - Односторонняя конверсия (EN→RU, нет RU→EN) - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Двусторонняя смена раскладки.
- **Окружение**: `LanguageDetector`, `DecisionEngine`, локальные словари.
- **Предпосылки**: Изначально проверялась валидность только в одной стороне.

### ❌ **Проблема:**
- **Симптомы**: Повторное нажатие хоткея не возвращало исходную раскладку.
- **Корневая причина**: Отсутствие корректной детекции языка и проверки в обе стороны.
- **Влияние**: UX‑регрессия.

### 🛠️ **Решение:**
- **Подход**: Детекция языка + валидация токена в обеих словарных базах.
- **Конкретные действия**: Реализован `LanguageDetector`, изменена логика решения в `DecisionEngine`.
- **Результат**: Стабильная двусторонняя конверсия одним хоткеем.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Комбинация positional mapping + словари.
- **❌ Что не сработало**: Наивная проверка только одной стороны.
- **💡 Уроки**: Всегда проверять обе гипотезы языка.
- **🔄 Альтернативы**: N‑gram/ML — избыточно для MVP.

### 🎯 **Применимость:**
- **Теги**: #backend #language #decision #dictionaries
- **Похожие ситуации**: Смешанный ввод.
- **Профилактика**: Unit‑тесты на round‑trip.

## 🔍 007 - 2025-08-09 - Смена системной раскладки после конверсии - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Синхронизировать системный input source с результатом конверсии.
- **Окружение**: `InputSourceManager` (TIS/HIToolbox), `SwitchAction`.
- **Предпосылки**: После конверсии текст менялся, но системная раскладка могла остаться «старой».

### ❌ **Проблема:**
- **Симптомы**: Раскладка текста и системная раскладка расходились.
- **Корневая причина**: Не выполнялся выбор нужного TIS input source.
- **Влияние**: Следующее слово печаталось не в той раскладке.

### 🛠️ **Решение:**
- **Подход**: Явный выбор input source после вычисления целевой раскладки.
- **Конкретные действия**: `InputSourceManager.select(layout:)` с корректным кастингом типов без кеширования.
- **Результат**: Системная раскладка синхронизируется с изменённым текстом.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Прямое использование TIS API.
- **❌ Что не сработало**: Кеширование источников усложняло и давало предупреждения.
- **💡 Уроки**: Лучше вызывать TIS напрямую при каждой операции.
- **🔄 Альтернативы**: Локальный кеш с валидацией — не нужен на MVP.

### 🎯 **Применимость:**
- **Теги**: #backend #macos #keyboard #tis
- **Похожие ситуации**: Переключение языков по контексту.
- **Профилактика**: Централизованный helper выбора раскладки.

## 🔍 008 - 2025-08-09 - Некорректное поведение с одиночными символами - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Конверсия одиночных символов (например, «ю» ↔ ".").
- **Окружение**: `KeyboardMonitor`, `SwitchAction`, `KeySender`.
- **Предпосылки**: Минимальная длина токена мешала конверсии и удаление/ввод сбивали позицию каретки.

### ❌ **Проблема:**
- **Симптомы**: Одиночные символы не конвертировались «после ввода», только при выделении.
- **Корневая причина**: minLength>1 и отсутствие корректного удаления перед печатью.
- **Влияние**: UX‑несоответствие.

### 🛠️ **Решение:**
- **Подход**: Разрешить длину 1 и аккуратно заменить последний символ.
- **Конкретные действия**: Снижен minLength до 1; добавлен `Shift+Left` перед печатью одного символа.
- **Результат**: Конверсия одиночных символов стала предсказуемой.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Минимальная инвазивность.
- **❌ Что не сработало**: Попытки угадать язык без удаления не помогали.
- **💡 Уроки**: Одиночные кейсы требуют спец‑ветки.
- **🔄 Альтернативы**: Манипуляция буфером приложения — нестабильно.

### 🎯 **Применимость:**
- **Теги**: #backend #text #edgecases
- **Похожие ситуации**: Замена последнего символа.
- **Профилактика**: Тесты на одиночные символы и пунктуацию.

## 🔍 009 - 2025-08-09 - Clipboard/AX тайминги в VSCode ломают вставку - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Обеспечить замену текста без зависимости от стабильности буфера обмена.
- **Окружение**: VS Code/Electron, AX API, NSPasteboard.
- **Предпосылки**: Наблюдались `has=no len=0`, вставка «съедала» текст.

### ❌ **Проблема:**
- **Симптомы**: Вставка через буфер не работала или работала нестабильно.
- **Корневая причина**: Тайминги и ограничения Electron‑поля ввода.
- **Влияние**: Невозможность надёжной замены.

### 🛠️ **Решение:**
- **Подход**: Перейти на HID‑печать и cut+type, восстановление Clipboard после операции.
- **Конкретные действия**: Отказ от `replaceSelectedText`, `replaceViaClipboardPaste`, `copySelectedTextViaClipboard` в пользу `cutSelectionAndType`.
- **Результат**: Стабильная работа без зависимости от Clipboard.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Полный контроль через HID‑typing.
- **❌ Что не сработало**: Любые clipboard‑вставки в VSCode.
- **💡 Уроки**: Clipboard — ненадёжный интерфейс для автоматизации.
- **🔄 Альтернативы**: IME‑плагин VSCode — вне области MVP.

### 🎯 **Применимость:**
- **Теги**: #backend #clipboard #electron #timing
- **Похожие ситуации**: Веб‑вью и iframes.
- **Профилактика**: Избегать зависимостей от Clipboard.

## 🔍 010 - 2025-08-12 - Невосстановление буфера после Cmd+V; переход на чисто событийное AX‑восстановление - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Функция AI‑трансформации текста по хоткею `Ctrl+Opt+T` с заменой выделения и корректным восстановлением исходного буфера обмена.
- **Окружение**: macOS Sonoma (Apple Silicon), `AccessibilityTextInteractor`, `AITransformAction`, `NSPasteboard`, `AXObserver`.
- **Предпосылки**: Вставка выполняется через симуляцию `Cmd+V`, требуется восстановить исходный буфер (включая rich‑types) строго по событию, без таймеров.

### ❌ **Проблема:**
- **Симптомы**: Исходный буфер обмена не всегда возвращался; иногда восстанавливался только после дополнительного пользовательского действия.
- **Корневая причина**:
  - Неполный бэкап (только `string`) приводил к потере rich‑форматов.
  - Восстановление было завязано на дополнительный флаг/событие после `kAXValueChanged`, из‑за чего требовалось «ещё одно действие».
- **Влияние**: Риск потери данных в Clipboard и нестабильный UX AI‑операции.

### 🛠️ **Решение:**
- **Подход**: Полный бэкап `NSPasteboard`: все `NSPasteboardItem` со всеми типами и данными + fallback строка. Восстановление триггерить немедленно по `AX kAXValueChangedNotification` целевого текстового элемента.
- **Конкретные действия**:
  1. Реализован полноформатный снимок буфера (массив `NSPasteboardItem` → `[PasteboardType: Data]` + `pasteBackupString`).
  2. Подписка `AXObserver` только на `kAXValueChangedNotification` для сфокусированного элемента; отказ от промежуточных флагов/ожиданий.
  3. В колбэке `kAXValueChanged` — немедленное восстановление Clipboard и деинициализация наблюдателя (без таймеров/задержек).
  4. Удалены неиспользуемые свойства/подписки (например, на `kAXSelectedTextChangedNotification`) и вспомогательные флаги.
- **Результат**: Восстановление буфера происходит сразу после фактической вставки; отпала необходимость в дополнительных действиях пользователя; rich‑форматы сохраняются.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Привязка к `kAXValueChanged` как надёжной точке завершения вставки; полноформатный бэкап/восстановление `NSPasteboardItem`.
- **❌ Что не сработало**: Любые задержки/таймеры и зависимость от вторичных сигналов/флагов после `kAXValueChanged`.
- **💡 Уроки**: Для операций, затрагивающих Clipboard, используем только событийную синхронизацию и сохраняем все типы данных, а не один `string`.
- **🔄 Альтернативы**: HID‑tap в качестве резервного триггера возможен, но усложняет и дублирует логику; при стабильном `kAXValueChanged` не требуется.

### ✅ **Верификация:**
- Шаги: Выбрать текст → запустить AI → дождаться замены → сразу вставить `Cmd+V` в другом месте.
- Ожидаемо: Вставляется исходное содержимое Clipboard (включая rich‑форматы).
- Диагностика: В логах при `SWITCHER_DEBUG=1` видно `[AXPaste] kAXValueChanged received; restoring clipboard immediately`.

### 🎯 **Применимость:**
- **Теги**: #macos #accessibility #clipboard #axobserver #ux
- **Похожие ситуации**: Любые автоматизации с `Cmd+V` и требованием сохранения исходного Clipboard.
- **Профилактика**: Никогда не ограничиваться `string`; бэкапить/восстанавливать все `NSPasteboardItem` и использовать только событийные триггеры.

## 🔍 011 - 2025-08-16 - Конфликт старой и новой систем хоткеев - 🏗️ Архитектурная

### 📊 **Контекст:**
- **Задача**: Восстановить работу хоткея для смены регистра (Shift+Control+Z) после рефакторинга.
- **Окружение**: macOS, Swift, `AppDelegate`, новая система `HotkeyManager`.
- **Предпосылки**: В проекте была внедрена новая централизованная система `HotkeyManager` для AI-действий, но старый механизм регистрации хоткеев в `AppDelegate` не был полностью удален.

### ❌ **Проблема:**
- **Симптомы**: Хоткей для смены регистра перестал работать. В логах не было упоминаний о его регистрации.
- **Корневая причина**: Старый и новый механизмы регистрации `CGEventTap` конфликтовали. `CaseTransformAction` использовал устаревший механизм регистрации в `AppDelegate`, который был заменен на `HotkeyManager`, но не был в него интегрирован.
- **Влияние**: Основная функциональность смены регистра была недоступна.

### 🛠️ **Решение:**
- **Подход**: Полностью перейти на новую систему `HotkeyManager` и удалить устаревшую логику.
- **Конкретные действия**:
  1. Модифицировал `HotkeyManager`, чтобы он мог регистрировать не только AI-действия, но и любые другие (например, смену регистра).
  2. Добавил в `HotkeyManager` метод `registerCaseTransform(hotkeyString:)`.
  3. В `AppDelegate` вызвал новый метод для регистрации хоткея смены регистра.
  4. Удалил старый код регистрации `CGEventTap` из `AppDelegate`.
- **Результат**: Вся логика горячих клавиш централизована в `HotkeyManager`, конфликт устранен, функциональность восстановлена.

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Централизация логики в `HotkeyManager` сделала систему более предсказуемой и расширяемой.
- **❌ Что не сработало**: Первоначальный анализ ошибочно сфокусировался на изменениях в `AccessibilityTextInteractor`, а не на глобальных изменениях в архитектуре (`AppDelegate`).
- **💡 Уроки**: При рефакторинге необходимо в первую очередь проверять точки интеграции и инициализации компонентов. Появление новых файлов (`HotkeyManager.swift`) — ключевой индикатор архитектурных изменений.
- **🔄 Альтернативы**: Можно было бы попытаться заставить работать оба `CGEventTap` параллельно, но это усложнило бы систему и могло привести к новым конфликтам.

### 🎯 **Применимость:**
- **Теги**: #архитектура #hotkey #eventtap #macos #swift #refactoring
- **Похожие ситуации**: Любой рефакторинг, связанный с заменой или централизацией ключевых систем (обработка событий, конфигурация, и т.д.).
- **Профилактика**: При добавлении новых систем управления необходимо сразу переносить в них всю связанную функциональность и удалять устаревший код.

## 🔍 013 - 2025-01-27 - Timing проблемы с первым токеном при запуске приложения - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Решить проблему отсутствия автоподмены для первого введенного слова после запуска приложения
- **Окружение**: macOS, Swift, CGEventTap, TIS (Text Input Source) API, KeyboardMonitor
- **Предпосылки**: Пользователь отметил, что после ввода первого слова и пробела автоподмена раскладки не происходит, но повторный ввод того же слова работает корректно

### ❌ **Проблема:**
- **Симптомы**: Первое слово после запуска приложения не подвергается автоподмене раскладки при нажатии пробела; повторные слова конвертируются нормально
- **Корневая причина**: Timing-проблемы между инициализацией CGEventTap и первым вводом текста, приводящие к тому, что TISCopyCurrentKeyboardInputSource() возвращает nil при первом вызове
- **Влияние**: Функция автоподмены не работает для самого первого токена, что ухудшает UX при запуске приложения

### 🛠️ **Решение:**
- **Подход**: Внедрить retry-механизм для получения раскладки с учетом timing проблем при запуске
- **Конкретные действия**:
  1. Добавлен флаг `isSystemReady` в KeyboardMonitor для отслеживания готовности системы
  2. Создана функция `getCurrentLayoutWithRetry()` с 3 попытками получения раскладки и задержкой 1ms между попытками
  3. Интегрирован retry-механизм в `performAutoSwitchIfEnabled()` вместо прямого вызова TIS API
  4. Добавлен сброс флага `isSystemReady = false` в `resetContext()` при смене приложения
- **Результат**: Автоподмена работает для первого токена, retry-механизм преодолевает timing проблемы запуска

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Retry-механизм с минимальными задержками usleep() вместо таймеров, что соответствует принципам @general.mdc
- **❌ Что не сработало**: Прямой вызов TISCopyCurrentKeyboardInputSource() без обработки ошибок при первом запуске
- **💡 Уроки**: При работе с системными API (особенно TIS) необходимо учитывать timing проблемы при запуске приложения; retry-механизмы должны быть событийными, а не основанными на таймерах
- **🔄 Альтернативы**: Можно было бы добавить задержку при запуске KeyboardMonitor, но это противоречило бы принципам event-driven архитектуры

### 🎯 **Применимость:**
- **Теги**: #macos #timing #tis #eventtap #keyboard #swift
- **Похожие ситуации**: Любые операции с TIS API или другими системными сервисами при первом запуске приложения
- **Профилактика**: Для системных API с timing зависимостями всегда реализовывать retry-механизмы; тестировать сценарии "первого использования" после запуска

---

## 🔍 014 - 2025-01-27 - Критическая ошибка в статическом анализе метода shouldConvert - 🏗️ Архитектурная

### 📊 **Контекст:**
- **Задача**: Анализ мертвого кода и дублирующейся логики в проекте PuntoSwitcher
- **Окружение**: Swift проект с архитектурой MVVM-like, использующий Accessibility API и TIS
- **Предпосылки**: Планировался рефакторинг для удаления неиспользуемого кода

### ❌ **Проблема:**
- **Симптомы**: Метод `shouldConvert(token: String, currentLanguage: String) -> Bool` в DecisionEngine был ошибочно классифицирован как мертвый код
- **Корневая причина**: Недостаточная глубина статического анализа - метод активно используется в `KeyboardMonitor.performAutoSwitchIfEnabled()` и `SwitcherCLI/main.swift`, но эти зависимости не были выявлены при первоначальном сканировании
- **Влияние**: Если бы метод был удален, произошла бы полная потеря функциональности автоподмены при вводе текста пользователем

### 🛠️ **Решение:**
- **Подход**: Глубокий анализ с использованием grep для поиска всех вызовов метода и ручная проверка зависимостей
- **Конкретные действия**: Выполнен поиск по всему проекту с использованием `grep -n "shouldConvert"` для выявления всех мест использования
- **Результат**: Метод был исключен из списка на удаление, шаг 1 рефакторинга отменен

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Использование grep позволило быстро найти все места использования
- **❌ Что не сработало**: Первоначальный статический анализ был недостаточно глубоким - не выявил косвенные зависимости через протоколы и делегаты
- **💡 Уроки**: Для критически важных методов необходимо проводить многоуровневый анализ: статический анализ + динамический поиск вызовов + анализ интерфейсов
- **🔄 Альтернативы**: Можно было бы использовать более продвинутые инструменты статического анализа (например, SourceKit-LSP), но они могут быть избыточны для небольших проектов

### 🎯 **Применимость:**
- **Теги**: #swift #refactoring #static-analysis #dependency-analysis #dead-code #swift
- **Похожие ситуации**: Любые рефакторинги с удалением кода требуют тщательного анализа зависимостей
- **Профилактика**: Всегда проводить grep-поиск по имени метода перед удалением; проверять интерфейсы и делегаты; тестировать базовую функциональность после анализа

---

## 🔍 015 - 2025-01-27 - Масштабная централизация создания компонентов через ComponentFactory - 🏗️ Архитектурная

### 📊 **Контекст:**
- **Задача**: Устранение дублирования логики создания DictionaryService, LayoutConverter, DecisionEngine в 7+ местах проекта
- **Окружение**: Swift проект с множественными точками входа (App, CLI, тесты)
- **Предпосылки**: Каждый модуль создавал компоненты самостоятельно, что приводило к дублированию кода и сложности поддержки

### ❌ **Проблема:**
- **Симптомы**: Один и тот же код создания компонентов повторялся в SwitchAction (3 места), KeyboardMonitor (1 место), CLI (1 место), test_simple.swift (1 место), test_autoswitch.swift (1 место), main.swift (1 место)
- **Корневая причина**: Отсутствие централизованной фабрики компонентов привело к дублированию логики инициализации
- **Влияние**: Сложность внесения изменений, повышенная вероятность ошибок при модификации логики создания компонентов

### 🛠️ **Решение:**
- **Подход**: Реализация Factory паттерна с thread-safe созданием, кэшированием и различными профилями конфигурации
- **Конкретные действия**:
  1. Создан `ComponentFactory.swift` с методами для разных сценариев использования
  2. Реализован thread-safe паттерн с использованием serial queue
  3. Добавлено кэширование для production и CLI компонентов
  4. Выполнена поэтапная миграция всех мест создания компонентов
- **Результат**: Устранено ~50 строк дублированного кода, улучшена поддерживаемость и тестируемость

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Поэтапная миграция позволила сохранить работоспособность на каждом этапе; thread-safe реализация обеспечила надежность
- **❌ Что не сработало**: Первоначально планировалось использовать Singleton, но это ограничило бы гибкость тестирования
- **💡 Уроки**: Factory паттерн эффективен для устранения дублирования; thread-safety критически важна для компонентов, используемых в разных контекстах
- **🔄 Альтернативы**: Можно было бы использовать Dependency Injection фреймворк (Swinject, Needle), но для небольшого проекта Factory паттерн оказался оптимальным

### 🎯 **Применимость:**
- **Теги**: #swift #refactoring #factory-pattern #dependency-injection #thread-safety #architecture #swift
- **Похожие ситуации**: Проекты с множественными точками входа и повторяющейся логикой создания объектов
- **Профилактика**: При проектировании учитывать все сценарии использования компонентов; использовать Factory паттерн для сложных объектов

---

## 🔍 016 - 2025-01-27 - Централизация логики получения текущей раскладки с retry-механизмом - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Устранение дублирования вызовов TIS API для получения текущей раскладки клавиатуры
- **Окружение**: macOS приложение с использованием Text Input Sources (TIS) API
- **Предпосылки**: Логика получения раскладки дублировалась в 4 местах без обработки timing проблем

### ❌ **Проблема:**
- **Симптомы**: Один и тот же код `TISCopyCurrentKeyboardInputSource()` + `TISGetInputSourceProperty()` повторялся в SwitchAction (2 места) и KeyboardMonitor (1 место)
- **Корневая причина**: Отсутствие централизованного метода для получения раскладки и игнорирование timing проблем при запуске приложения
- **Влияние**: Потенциальные race conditions при быстрой смене раскладок, ошибки при первом запуске

### 🛠️ **Решение:**
- **Подход**: Создание централизованного метода в InputSourceManager с retry-механизмом для решения timing проблем
- **Конкретные действия**:
  1. Добавлен метод `getCurrentLayout()` в InputSourceManager с 3 попытками и задержкой 1ms
  2. Заменены все дублирующие вызовы в SwitchAction и KeyboardMonitor
  3. Удален приватный метод getCurrentLayoutWithRetry() из KeyboardMonitor
- **Результат**: Устранено ~25 строк дублированного кода, повышена надежность получения раскладки

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Retry-механизм решил timing проблемы при запуске; централизация упростила поддержку
- **❌ Что не сработало**: Первоначально планировалось использовать более длительные задержки, но 1ms оказалось достаточно
- **💡 Уроки**: Системные API (особенно TIS) требуют специальной обработки timing проблем; retry-механизмы должны быть частью архитектуры
- **🔄 Альтернативы**: Можно было бы использовать notifications от системы о смене раскладки, но это усложнило бы архитектуру

### 🎯 **Применимость:**
- **Теги**: #macos #tis #timing #retry-mechanism #api-design #keyboard-layout #swift
- **Похожие ситуации**: Работа с системными API, имеющими timing зависимости при запуске приложения
- **Профилактика**: Для системных API всегда реализовывать retry-механизмы; тестировать сценарии первого использования после холодного запуска

---

## 🔍 017 - 2025-08-28 - Бесконечный цикл в realtime автоподмене - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Реализация realtime префиксной автоподмены с немедленной заменой после 3 символов
- **Окружение**: macOS, Swift, KeyboardMonitor, CGEventTap, RealtimePrefixDecisionEngine
- **Предпосылки**: performRealtimeSubstitution() использует KeySender.typeText() для замены текста

### ❌ **Проблема:**
- **Симптомы**: "hel" → "hel" → "hel" в бесконечном цикле после первой конвертации
- **Корневая причина**: KeyboardMonitor перехватывает собственные HID события от KeySender.typeText(), что приводит к повторной обработке уже конвертированного токена
- **Влияние**: Полная неработоспособность realtime автоподмены

### 🛠️ **Решение:**
- **Подход**: Иммунизация токена для предотвращения повторной обработки
- **Конкретные действия**: Добавлен флаг isNextTokenImmune, который устанавливается после успешной конвертации и проверяется в performRealtimePrefixCheck()
- **Результат**: Бесконечный цикл устранен, система работает стабильно

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Простой флаг иммунизации эффективно решил проблему самоперехвата
- **❌ Что не сработало**: Попытки фильтровать события по userData или timestamp не работали надежно
- **💡 Уроки**: HID события требуют защиты от self-capture при эмуляции клавиш
- **🔄 Альтернативы**: Временная пауза eventTap, но это нарушило бы event-driven принципы

### 🎯 **Применимость:**
- **Теги**: #eventtap #keyboard #self-capture #realtime #swift #macos
- **Похожие ситуации**: Любые системы с HID эмуляцией клавиш и одновременным мониторингом
- **Профилактика**: Всегда реализовывать механизм предотвращения self-capture в event-driven системах

## 🔍 018 - 2025-08-28 - Раскладка не переключается после realtime автоподмены - 🏗️ Архитектурная

### 📊 **Контекст:**
- **Задача**: Обеспечить корректную раскладку клавиатуры после realtime конвертации
- **Окружение**: macOS, TIS API, InputSourceManager, реализация "руд" → "hel"
- **Предпосылки**: Система корректно конвертирует текст, но не переключает системную раскладку

### ❌ **Проблема:**
- **Симптомы**: После "руд" → "hel" пользователь продолжал печатать в русской раскладке: "приdtn" вместо "привет"
- **Корневая причина**: performRealtimeSubstitution() меняет только текст, но не переключает input source
- **Влияние**: Некорректное продолжение ввода, плохой UX

### 🛠️ **Решение:**
- **Подход**: Расширить PrefixConversionResult для включения целевой раскладки и переключать ее в performRealtimeSubstitution()
- **Конкретные действия**: Добавлен targetLayout в PrefixConversionResult.convert, интегрирован InputSourceManager.select() в performRealtimeSubstitution()
- **Результат**: Система корректно переключает раскладку после конвертации

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Расширение result enum позволило передать дополнительный контекст
- **❌ Что не сработало**: Первоначальная архитектура не учитывала необходимость переключения контекста ввода
- **💡 Уроки**: Автоподмена должна включать переключение всего контекста ввода, не только текста
- **🔄 Альтернативы**: Отдельный вызов после подмены, но это усложнило бы координацию

### 🎯 **Применимость:**
- **Теги**: #tis #keyboard-layout #realtime #architecture #input-source #swift #macos
- **Похожие ситуации**: Любые системы автоматической коррекции с переключением языков
- **Профилактика**: При проектировании автоподмены всегда учитывать весь контекст ввода

## 🔍 019 - 2025-08-28 - Потеря пробелов после realtime автоподмены - 🏗️ Архитектурная

### 📊 **Контекст:**
- **Задача**: Сохранение boundary символов (пробелов) между realtime-конвертированными словами
- **Окружение**: KeyboardMonitor, KeySender.selectPreviousCharacter(), boundary handling
- **Предпосылки**: "привет hello как" должно оставаться с пробелами между словами

### ❌ **Проблема:**
- **Симптомы**: "привет hello как" становилось "приветhelloкак" - исчезновение пробелов
- **Корневая причина**: KeySender.selectPreviousCharacter() захватывал boundary символы при выделении для замены
- **Влияние**: Склеивание слов, нарушение читаемости текста

### 🛠️ **Решение:**
- **Подход**: Hybrid approach с немедленным восстановлением boundary символов
- **Конкретные действия**: Если !lastCommittedToken.isEmpty, то KeySender.typeText(" ") перед конвертированным токеном; обновление lastCommittedToken = convertedToken после подмены
- **Результат**: Пробелы корректно сохраняются между конвертированными словами

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Немедленное восстановление boundary без сложной логики отложенного восстановления
- **❌ Что не сработало**: Первоначальная попытка с pendingBoundaryRestoration усложняла архитектуру
- **💡 Уроки**: Realtime операции должны сохранять boundary контекст немедленно
- **🔄 Альтернативы**: AX API для получения контекста, но это менее надежно в разных приложениях

### 🎯 **Применимость:**
- **Теги**: #boundary #text-processing #realtime #keyboard #ux #swift
- **Похожие ситуации**: Автокоррекция, spell-checking, любые realtime text transformations
- **Профилактика**: При realtime обработке текста всегда сохранять и восстанавливать boundary контекст

## 🔍 020 - 2025-08-28 - Дублирование пробелов при boundary restoration - 🔧 Техническая

### 📊 **Контекст:**
- **Задача**: Предотвращение дублирования boundary событий после реализации space restoration
- **Окружение**: KeyboardMonitor, boundary event handling, performRealtimeSubstitution()
- **Предпосылки**: После добавления space restoration появились двойные пробелы

### ❌ **Проблема:**
- **Симптомы**: "привет  hello" (двойные пробелы) после realtime конвертации
- **Корневая причина**: Восстановление пробела через KeySender.typeText(" ") + обработка оригинального boundary события пользователя
- **Влияние**: Визуальные артефакты, некорректное форматирование текста

### 🛠️ **Решение:**
- **Подход**: Consuming оригинального boundary события для предотвращения дублирования
- **Конкретные действия**: В handle() после восстановления пробела изменить return true на return false для поглощения события
- **Результат**: Устранены двойные пробелы, boundary события обрабатываются корректно

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Простое consuming события решило проблему без усложнения архитектуры
- **❌ Что не сработало**: Попытки фильтровать события по типу не работали надежно
- **💡 Уроки**: Boundary restoration требует prevention дублирования через event consuming
- **🔄 Альтернативы**: Флаги для отслеживания восстановленных событий, но это усложнило бы логику

### 🎯 **Применимость:**
- **Теги**: #event-handling #boundary #duplication #keyboard #event-consuming #swift
- **Похожие ситуации**: Любые системы с восстановлением пользовательских событий
- **Профилактика**: При восстановлении событий всегда предусматривать механизм предотвращения дублирования

## 🔍 021 - 2025-08-28 - Зависание приложения на вводе "rfr" - 🏗️ Архитектурная

### 📊 **Контекст:**
- **Задача**: Решение критического зависания при определенных комбинациях символов
- **Окружение**: RealtimePrefixDecisionEngine, KeyboardMonitor, currentToken state management
- **Предпосылки**: При вводе "rfr" приложение полностью зависало

### ❌ **Проблема:**
- **Симптомы**: Полная неотзывчивость UI и event processing при вводе "rfr"
- **Корневая причина**: currentToken не очищался после realtime подмены, система продолжала re-evaluate уже конвертированный текст в бесконечном цикле
- **Влияние**: Критическая неработоспособность приложения для определенных input patterns

### 🛠️ **Решение:**
- **Подход**: Архитектурное решение с четким разделением состояний до/после операций
- **Конкретные действия**: currentToken и currentTokenMeta очищаются immediately после KeySender.typeText() в performRealtimeSubstitution(), отказ от "костылей" в пользу архитектурного решения
- **Результат**: Устранено зависание, система стабильна для всех input patterns

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Архитектурное решение с четким lifecycle management состояния
- **❌ Что не сработало**: Первоначальный "костыль" с агрессивным reset при identical conversion
- **💡 Уроки**: Event-driven системы должны четко разделять состояния до/после операций; избегать "костылей" в пользу системных решений
- **🔄 Альтернативы**: Таймауты или circuit breakers, но они противоречат event-driven принципам

### 🎯 **Применимость:**
- **Teги**: #architecture #state-management #lifecycle #event-driven #realtime #swift
- **Похожие ситуации**: Любые event-driven системы с state mutations и potential re-evaluation
- **Профилактика**: Четко определять lifecycle состояний; тестировать edge cases с циклическими patterns

## 🔍 022 - 2025-08-28 - Неожиданное поведение логики ожидания (.wait) - 🏗️ Архитектурная

### 📊 **Контекст:**
- **Задача**: Диагностика почему префикс "руд" не вызывает .wait, а сразу конвертируется
- **Окружение**: RealtimePrefixDecisionEngine, DictionaryService.hasPrefixInLanguage(), словари
- **Предпосылки**: Ожидание что "руд" и "hel" присутствуют в обоих словарях → .wait

### ❌ **Проблема:**
- **Симптомы**: .convert вместо ожидаемого .wait для префикса "руд"
- **Корневая причина**: Архитектурный анализ показал, что hasInCurrent=false для "руд" в русском словаре - система работает правильно согласно логике
- **Влияние**: Первоначальное непонимание корректности поведения системы

### 🛠️ **Решение:**
- **Подход**: Глубокая диагностика с добавлением debug логирования для анализа словарного содержимого
- **Конкретные действия**: Добавлены детальные print в DictionaryService и RealtimePrefixDecisionEngine для анализа hasInCurrent/hasInOpposite значений
- **Результат**: Подтверждено что система работает правильно - в словаре действительно нет слов с префиксом "руд"

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Диагностическое логирование позволило точно определить причину поведения
- **❌ Что не сработало**: Предположения о содержимом словаря без verification
- **💡 Уроки**: Unexpected behavior требует verification assumptions о данных; диагностические инструменты critical для понимания системы
- **🔄 Альтернативы**: Unit тесты на содержимое словарей, но диагностика была быстрее

### 🎯 **Применимость:**
- **Теги**: #debugging #dictionary #assumptions #verification #data-analysis #swift
- **Похожие ситуации**: Любые случаи unexpected behavior с внешними данными (словари, API, файлы)
- **Профилактика**: Всегда верифицировать assumptions о данных; добавлять диагностические capabilities

## 🔍 023 - 2025-08-28 - Неверные предположения о содержимом словаря - 🤝 Коммуникационная

### 📊 **Контекст:**
- **Задача**: Понимание почему логика .wait не работает для сценария "руд"
- **Окружение**: Анализ русского словаря, понимание prefix matching алгоритма
- **Предпосылки**: Предположение что "рудник" в словаре означает наличие префикса "руд"

### ❌ **Проблема:**
- **Симптомы**: Ожидание .wait behavior для "руд", но получение .convert
- **Корневая причина**: Коммуникационная ошибка - неверное understanding словарного содержимого. Слова "труд", "грудной", "пруд" содержат "руд", но НЕ начинаются с этого префикса
- **Влияние**: Время потрачено на диагностику "проблемы", которая оказалась корректным поведением

### 🛠️ **Решение:**
- **Подход**: Детальный анализ содержимого словаря и понимание prefix vs substring различий
- **Конкретные действия**: Анализ показал что "труд" начинается с "тру", "грудной" с "гру", "пруд" с "пру" - НЕТ слов с префиксом "руд"
- **Результат**: Понимание что система работает корректно, .convert behavior обоснован

### 📈 **Анализ:**
- **✅ Что сработало хорошо**: Systematic approach к анализу данных через детальное логирование
- **❌ Что не сработало**: Assumptions без verification данных привели к неверному пониманию проблемы
- **💡 Уроки**: Dictionary debugging требует content inspection, не assumptions; prefix vs substring - важное различие
- **🔄 Альтернативы**: Предварительный анализ словарей через scripts, но direct debugging был эффективнее

### 🎯 **Применимость:**
- **Теги**: #communication #assumptions #dictionary #data-analysis #debugging #requirements
- **Похожие ситуации**: Анализ любых внешних данных где важны точные алгоритмы поиска
- **Профилактика**: Всегда верифицировать understanding данных; различать similar но different концепции (prefix vs substring)

---

*Записи добавляются только по запросу пользователя*


